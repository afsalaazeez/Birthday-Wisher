// ********RoostGPT********
// Test generated by RoostGPT for test react-bday using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render } from '@testing-library/react';
import Countdown from '../../src/Countdown';

describe('Countdown Component', () => {
    test('renders the countdown correctly when it is not the birthday', () => {
        // Setup the props for not birthday
        const countdownData = {
            isItBday: false,
            days: 5,
            hours: 10,
            minutes: 20,
            seconds: 30,
        };

        // Render the component
        const { getByText } = render(<Countdown countdownData={countdownData} name="John Doe" />);
        
        // Assertions to check if countdown is displayed correctly
        expect(getByText(/5 Days/i)).toBeInTheDocument();
        expect(getByText(/10 Hours/i)).toBeInTheDocument();
        expect(getByText(/20 Minutes/i)).toBeInTheDocument();
        expect(getByText(/30 Seconds/i)).toBeInTheDocument();
    });

    test('renders birthday wish when it is the birthday', () => {
        // Setup the props for birthday
        const countdownData = {
            isItBday: true,
        };

        // Mock the Wish component to keep test isolated
        jest.mock('../../src/Wish', () => () => <div>Birthday Wish</div>);

        // Render the component
        const { getByText } = render(<Countdown countdownData={countdownData} name="Jane Doe" />);
        
        // Assertions to check if birthday wish is displayed
        expect(getByText(/Birthday Wish/i)).toBeInTheDocument();
    });
});

