// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render } from '@testing-library/react';
import Wish from '../../Wish'; // Ensure the import path is correct relative to the test file location

describe('Wish Component', () => {
  beforeEach(() => {
    // This function runs before each test. You can add setup code here if needed.
  });

  test('should render without crashing', () => {
    const { getByText } = render(<Wish name="John Doe" />);
    expect(getByText(/happy birthday john doe/i)).toBeInTheDocument();
  });

  test('should display name in uppercase', () => {
    const { getByText } = render(<Wish name="Jane" />);
    expect(getByText('JANE')).toBeInTheDocument();
  });

  afterEach(() => {
    // This function runs after each test. You can add cleanup code here if needed.
  });
});

