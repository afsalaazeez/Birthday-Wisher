// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render, screen } from '@testing-library/react';
import Wish from '../../Wish.jsx';

describe('Wish component', () => {
  test('renders with name prop', () => {
    render(<Wish name="Alice" />);
    const wishElement = screen.getByText(/happy birthday alice !!!/i);
    expect(wishElement).toBeInTheDocument();
  });

  test('displays the name in uppercase', () => {
    render(<Wish name="Bob" />);
    const highlightedName = screen.getByText('BOB');
    expect(highlightedName).toHaveClass('highlight');
    expect(highlightedName).toHaveTextContent('BOB');
  });

  test('contains the static text "HAPPY BIRTHDAY"', () => {
    render(<Wish name="Charlie" />);
    const staticTextElement = screen.getByText(/happy birthday/i);
    expect(staticTextElement).toBeInTheDocument();
  });
});

