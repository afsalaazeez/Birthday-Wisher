// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import Birthday from '../../Birthday';

describe('Birthday component', () => {
  test('renders correctly with default props', () => {
    render(<Birthday />);
    expect(screen.getByTestId('birthday-component')).toBeInTheDocument();
  });

  test('displays the correct birthday message', () => {
    const name = 'John';
    const age = 30;
    render(<Birthday name={name} age={age} />);
    expect(screen.getByText(`Happy Birthday ${name}! You are now ${age} years old.`)).toBeInTheDocument();
  });

  // Additional tests can include interaction tests, prop changes, and edge cases, etc.
  // ...
});


